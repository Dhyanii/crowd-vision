# CrowdVision – Python dependencies
# Install with: pip install -r requirements.txt

# ── Core detection ──────────────────────────────
ultralytics==8.2.18         # YOLOv8 (includes torch as transitive dep)

# ── API server ───────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9     # Required for file uploads in FastAPI

# ── Image / video processing ─────────────────────
opencv-python-headless==4.9.0.80
numpy>=1.24,<2.0

# ── Optional extras ──────────────────────────────
# torch==2.3.0              # Pin if you need a specific CUDA version
# torchvision==0.18.0